<?xml version="1.0" encoding="utf-8"?>
<!-- PROSM Time - HTTPS only, and no user-installed CA is trusted in a
     release build (blocks casual traffic interception / API scraping
     with a proxy tool). Debug builds still trust the debug store so
     local development is unaffected. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
